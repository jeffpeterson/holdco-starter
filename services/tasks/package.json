{
  "name": "holdco-tasks",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply holdco-tasks --remote",
    "migrate:local": "wrangler d1 migrations apply holdco-tasks --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
