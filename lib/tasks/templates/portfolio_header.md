# Portfolio

> **GENERATED FILE — do not hand-edit.** Regenerated by `bin/holdco index` from the
> per-venture files under `ventures/`. To change a venture, edit its file in `ventures/`
> (or use `bin/holdco new|register`) and re-run the index.

Every business in the portfolio, grouped by stage. Each is its own self-running operator
repo (its own `AGENTS.md`, persona, backlog, and `./<name>` launcher) — holdco oversees
them, allocates attention, and stamps out new ones. See `docs/PLAYBOOK.md` for the
repeatable start-a-business flow, and `tasks/` (indexed by `TASKS.md`) for portfolio-level
work.

Spin up the next one:  `bin/holdco new <name> "Display Title" "one-line tagline"`

---
